<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        The XML theme exists only to give the window a correct background before the
        first Compose frame, and to satisfy AppCompat (needed for per-app languages on
        API < 33). All actual styling lives in the Compose design system.
    -->
    <style name="Theme.Journalator" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
